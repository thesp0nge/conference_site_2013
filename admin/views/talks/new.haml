%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:talks, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:talks, :new)

.tabs-content
  -form_for :talk, url(:talks, :create), :class => 'form-horizontal' do |f|
    =partial 'talks/form', :locals => { :f => f }
