%ul.nav.nav-tabs
  %li.active=link_to tag_icon(:list, pat(:list)), url(:talks, :index)
  %li=link_to tag_icon(:plus, pat(:new)), url(:talks, :new)

.tabs-content
  %table#list.table.table-striped.table-hover.table-condensed
    %thead
      %tr
        %th.header.list-menu
          .dropdown
            %a.list-menu-toggle.dropdown-toggle{:href => '#', :'data-toggle' => :dropdown}
              =tag_icon :magic
              %span.caret
            %ul.list-menu.dropdown-menu{:role => :menu}
              %li.list-menu-wrapper=link_to tag_icon(:check, pat(:select_all)), '#', :id => 'select-all', :class => 'list-menu-link'
              %li.list-menu-wrapper.list-menu-wrapper-disabled
                =link_to tag_icon(:'check-empty', pat(:deselect_all)), '#', :id => 'deselect-all', :class => 'list-menu-link list-menu-link-disabled'
              %li.list-menu-divider.divider
              %li.list-menu-wrapper.list-menu-wrapper-disabled
                =link_to tag_icon(:trash, pat(:delete_selected)), '#', :id => 'delete-selected', :class => 'list-menu-link list-menu-link-disabled'
                .list-menu-popover-delete-selected.popover.right
                  .arrow
                  %h3.popover-title=pat(:delete_selected_title)
                  .popover-content
                    -form_tag url(:talks, :destroy_many), :method => :delete do
                      =hidden_field_tag :talk_ids, :'data-delete-many-ids' => true
                      =submit_tag pat(:delete), :class =>'list-menu-popover-delete-selected-btn btn btn-danger btn-small'
                      .btn.btn-small.cancel=pat(:cancel)
        %th.header= :id
        %th.header= :title
        %th.header= :abstract
        %th.header= :slides
        %th.header= :created_at
        %th.header= :updated_at
        %th.header= :speaker_id
        %th.header= :slot_id
        %th.header.list-row-action-header

    %tbody
      -@talks.each do |talk|
        %tr.list-row
          %td.list-column.list-selectable
            =check_box_tag 'talk_ids[]', :value => talk.id, :class => 'list-selectable-checkbox'
          %td.list-column=talk.id
          %td.list-column=talk.title
          %td.list-column=talk.abstract
          %td.list-column=talk.slides
          %td.list-column=talk.created_at
          %td.list-column=talk.updated_at
          %td.list-column=talk.speaker_id
          %td.list-column=talk.slot_id
          %td.list-column.list-row-action
            .list-row-action-wrapper
              =link_to tag_icon(:edit), :href => url(:talks, :edit, :id => talk.id), :rel => :tooltip, :title => "#{pat(:edit)} talk", :class => 'list-row-action-wrapper-link'
              =link_to tag_icon(:trash), :rel => :tooltip, :title => "#{pat(:delete)} talk", :class => 'list-row-action-delete-one list-row-action-wrapper-link'
              .popover.list-row-action-popover-delete-one.left
                .arrow
                %h3.popover-title=pat(:delete, :model => "talk")
                .popover-content
                  -form_tag url(:talks, :destroy, :id => talk.id), :method => :delete do
                    =submit_tag pat(:delete), :class =>'list-row-action-popover-delete-one-btn btn btn-danger btn-small'
                    .btn.btn-small.cancel=pat(:cancel)
