%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:speakers, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:speakers, :new)

.tabs-content
  -form_for :speaker, url(:speakers, :create), :class => 'form-horizontal' do |f|
    =partial 'speakers/form', :locals => { :f => f }
